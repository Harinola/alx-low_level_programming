#include "main.h"
/**
 * set_string - sets the value of a pointer to a char.
 * @s: the address of the pointer whose content is to be changed.
 * @to: the address of the content to be changed to.
 *
 * Return: void.
 */
void set_string(char **s, char *to)
{
		*s = to;

}
